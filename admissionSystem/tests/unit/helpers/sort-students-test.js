import { sortStudents } from '../../../helpers/sort-students';
import { module, test } from 'qunit';

module('Unit | Helper | sort students');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sortStudents(42);
  assert.ok(result);
});
