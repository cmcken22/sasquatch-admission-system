import { assignRole } from '../../../helpers/assign-role';
import { module, test } from 'qunit';

module('Unit | Helper | assign role');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = assignRole(42);
  assert.ok(result);
});
