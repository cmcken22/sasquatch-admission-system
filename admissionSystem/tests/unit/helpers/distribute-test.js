import { distribute } from '../../../helpers/distribute';
import { module, test } from 'qunit';

module('Unit | Helper | distribute');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = distribute(42);
  assert.ok(result);
});
