import { calculateAVG } from '../../../helpers/calculate-avg';
import { module, test } from 'qunit';

module('Unit | Helper | calculate avg');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = calculateAVG(42);
  assert.ok(result);
});
