import { validateStudentForm } from '../../../helpers/validate-student-form';
import { module, test } from 'qunit';

module('Unit | Helper | validate student form');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = validateStudentForm(42);
  assert.ok(result);
});
