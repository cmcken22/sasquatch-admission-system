import { parseDate } from '../../../helpers/parse-date';
import { module, test } from 'qunit';

module('Unit | Helper | parse date');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = parseDate(42);
  assert.ok(result);
});
